There's your template. A few things worth noting:

**Built-in components ready to use:**
- Three callout box styles — green tip, orange warning, blue info
- Code blocks (full block and inline)
- Image + caption pattern (commented out, just uncomment)
- Post footer nav back to blog index and ProDirt apps

**Your workflow for each new post:**
1. Duplicate this file, rename it something SEO-friendly like `ntfs-inheritance-broken-fix.html`
2. Update the `<title>`, `<meta description>`, date, tag, h1, and subtitle at the top
3. Write the body — the section structure is already there as a guide
4. Drop it in `/blog/posts/`
5. Add one line to `posts.json`
6. Push

The section structure in the template — problem, what I was dealing with, the fix, result, wrap up — is deliberately written to match the kind of "undocumented problem solved" posts you mentioned earlier. That format tends to rank well because it mirrors exactly what someone types into Google when they're stuck.

Want to tackle updating the footer links across your app pages next, or jump straight into writing the first post?
